import React from 'react';

const navbar = (props) => {
  return (
    <div>
      <button>Home</button>
      <button>Create New Itinerary</button>
      <button>My Itineraries</button>
    </div>
  )
}

export default navbar;
